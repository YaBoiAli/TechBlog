const router = require("express").Router();
const { Post, Comment, User } = require("../models");

// dashboard
router.get('/', (req, res) => {

})

// new post route


// edit post route


module.exports = router;